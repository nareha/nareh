import '../../global.css';

export const Help = () => {
    return (
        <div className="command">
            available commands: about, banner, bash, funfact, help, ping, repo
        </div>
    );
};
